## Strict AI manual test plan

- "Hva gjør jeg ved brann?" -> svar fra instruks + `source` med tittel, oppdatert dato (YYYY-MM-DD), og link "Klikk for å åpne: <tittel>".
- "hvordan lager jeg epplekake?" -> eksakt fallback-setning og ingen source/link.
